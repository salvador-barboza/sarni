programa papito;
var
  int n;

funcion int facrecursivo(int n)
{
  escribe(n, "\n");

  si (n > 1) entonces {
		regresa n * facrecursivo(n - 1);
	} sino {
		regresa 1;
	}

  regresa 0;
}

principal() {
  lee(n);
  n = facrecursivo(n);
  escribe(n, "\n");
}