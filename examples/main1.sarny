programa papito;
var
  int W, I;
  int  Arreglo2[8][10], Arreglo[10];

funcion int suma3(int i)
{
  si (i / 2 == 4.0) entonces {
    regresa 0;
  } sino {
    regresa i;
  }

  regresa 0;
}

principal() {
  desde W = 0 hasta 9 hacer {
    Arreglo[W] = W*10;
  }

  desde W = 0 hasta 9 hacer {
    escribe(Arreglo[W]);
  }
}